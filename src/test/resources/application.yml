spring:
  application:
    name: task-management-system
  datasource:
    url: jdbc:h2:mem:test
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: create
    show-sql: true

logging:
  level:
    web: debug